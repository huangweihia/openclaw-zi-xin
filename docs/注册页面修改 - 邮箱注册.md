# 🦀 OpenClaw 智信 - 注册页面修改为邮箱注册

> 修改时间：2026-04-02 14:55  
> 原因：按照产品要求，使用邮箱注册而非手机号

---

## 📋 修改内容

### 1. 修改注册页面视图

**文件：** `resources/views/max/auth/register.blade.php`

**修改：**
- ❌ 删除：手机号输入框
- ❌ 删除：短信验证码
- ✅ 添加：邮箱输入框
- ✅ 添加：密码输入框
- ✅ 添加：确认密码输入框

### 2. 修改注册控制器

**文件：** `app/Http/Controllers/Auth/RegisterController.php`

**修改：**
- ❌ 删除：`sendSmsCode()` 方法（短信验证码）
- ✅ 修改：`register()` 方法验证规则
- ✅ 修改：用户创建逻辑

---

## 🐳 Docker 执行命令

### 步骤 1：备份原文件

```bash
docker exec ai_side_php_max cp /var/www/html/resources/views/max/auth/register.blade.php /var/www/html/resources/views/max/auth/register.blade.php.bak
docker exec ai_side_php_max cp /var/www/html/app/Http/Controllers/Auth/RegisterController.php /var/www/html/app/Http/Controllers/Auth/RegisterController.php.bak
```

### 步骤 2：创建新的注册页面（邮箱注册）

```bash
docker exec ai_side_php_max cat > /var/www/html/resources/views/max/auth/register.blade.php << 'BLADEEOF'
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>免费注册 - OpenClaw 智信</title>
    @include('max.partials.head')
</head>
<body class="bg-gray-50">
    @php $currentPage = 'register'; @endphp
    @include('max.partials.nav')

    <!-- 注册表单 -->
    <div class="min-h-screen flex items-center justify-center py-12 px-4">
        <div class="max-w-md w-full">
            <!-- Logo 和标题 -->
            <div class="text-center mb-8">
                <h1 class="text-3xl font-bold gradient-bg bg-clip-text text-transparent mb-2">
                    免费注册 OpenClaw 智信
                </h1>
                <p class="text-gray-600">
                    OpenClaw + AI 智能体：把分散的信息差转化为可交付的咨询洞察
                </p>
            </div>

            <!-- 注册卡片 -->
            <div class="bg-white rounded-2xl shadow-xl p-8">
                @if(session('success'))
                    <div class="bg-green-50 text-green-700 px-4 py-3 rounded-lg mb-4">
                        {{ session('success') }}
                    </div>
                @endif

                @if($errors->any())
                    <div class="bg-red-50 text-red-700 px-4 py-3 rounded-lg mb-4">
                        @foreach($errors->all() as $error)
                            <div>{{ $error }}</div>
                        @endforeach
                    </div>
                @endif

                <form method="POST" action="{{ route('register') }}" id="registerForm">
                    @csrf
                    
                    <!-- 邮箱 -->
                    <div class="mb-4">
                        <label for="email" class="block text-sm font-medium text-gray-700 mb-2">
                            邮箱地址
                        </label>
                        <input 
                            type="email" 
                            id="email" 
                            name="email" 
                            required
                            placeholder="your@email.com"
                            class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-transparent"
                        >
                    </div>

                    <!-- 密码 -->
                    <div class="mb-4">
                        <label for="password" class="block text-sm font-medium text-gray-700 mb-2">
                            密码
                        </label>
                        <input 
                            type="password" 
                            id="password" 
                            name="password" 
                            required
                            minlength="6"
                            placeholder="至少 6 位密码"
                            class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-transparent"
                        >
                    </div>

                    <!-- 确认密码 -->
                    <div class="mb-4">
                        <label for="password_confirmation" class="block text-sm font-medium text-gray-700 mb-2">
                            确认密码
                        </label>
                        <input 
                            type="password" 
                            id="password_confirmation" 
                            name="password_confirmation" 
                            required
                            minlength="6"
                            placeholder="再次输入密码"
                            class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-transparent"
                        >
                    </div>

                    <!-- 昵称（可选） -->
                    <div class="mb-6">
                        <label for="name" class="block text-sm font-medium text-gray-700 mb-2">
                            昵称 <span class="text-gray-400">（可选）</span>
                        </label>
                        <input 
                            type="text" 
                            id="name" 
                            name="name" 
                            placeholder="给自己起个名字吧"
                            class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-transparent"
                        >
                    </div>

                    <!-- 注册按钮 -->
                    <button 
                        type="submit" 
                        class="w-full gradient-bg text-white py-3 rounded-lg font-semibold hover:opacity-90 transition mb-4"
                    >
                        同意协议并注册
                    </button>

                    <!-- 分隔线 -->
                    <div class="relative my-6">
                        <div class="absolute inset-0 flex items-center">
                            <div class="w-full border-t border-gray-300"></div>
                        </div>
                        <div class="relative flex justify-center text-sm">
                            <span class="px-2 bg-white text-gray-500">其他方式</span>
                        </div>
                    </div>

                    <!-- 微信扫码 -->
                    <button 
                        type="button" 
                        class="w-full bg-green-500 text-white py-3 rounded-lg font-semibold hover:bg-green-600 transition flex items-center justify-center space-x-2"
                    >
                        <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
                            <path d="M8.5 10.5h1v1h-1v-1zm5 0h1v1h-1v-1z"/>
                            <path d="M12 2C6.48 2 2 6.48 2 12c0 1.54.36 3 1 4.3V22l4.1-1.1C8.14 21.6 10.04 22 12 22c5.52 0 10-4.48 10-10S17.52 2 12 2zm0 18c-1.7 0-3.3-.4-4.8-1.1L4 20l1.1-3.2C4.4 15.5 4 13.8 4 12c0-4.4 3.6-8 8-8s8 3.6 8 8-3.6 8-8 8z"/>
                        </svg>
                        <span>微信扫码注册</span>
                    </button>
                </form>

                <!-- 登录链接 -->
                <p class="text-center mt-6 text-gray-600">
                    已有账号？
                    <a href="/login" class="text-purple-600 hover:text-purple-700 font-semibold">
                        立即登录
                    </a>
                </p>
            </div>

            <!-- 权益说明 -->
            <div class="mt-8 text-center">
                <p class="text-gray-600 mb-4">注册即享免费权益</p>
                <div class="grid grid-cols-3 gap-4">
                    <div class="bg-white p-4 rounded-lg shadow">
                        <div class="text-2xl mb-2">📰</div>
                        <div class="text-sm text-gray-700">每日 AI 资讯</div>
                    </div>
                    <div class="bg-white p-4 rounded-lg shadow">
                        <div class="text-2xl mb-2">📚</div>
                        <div class="text-sm text-gray-700">基础教程</div>
                    </div>
                    <div class="bg-white p-4 rounded-lg shadow">
                        <div class="text-2xl mb-2">📧</div>
                        <div class="text-sm text-gray-700">每周日报</div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    @include('max.partials.footer')
</body>
</html>
BLADEEOF
```

### 步骤 3：修改注册控制器

```bash
docker exec ai_side_php_max cat > /var/www/html/app/Http/Controllers/Auth/RegisterController.php << 'PHPEOF'
<?php

namespace App\Http\Controllers\Auth;

use App\Http\Controllers\Controller;
use App\Models\User;
use Illuminate\Http\Request;
use Illuminate\Support\Facades\Auth;
use Illuminate\Support\Facades\Log;

class RegisterController extends Controller
{
    /**
     * 显示注册表单 - MAX 版本（邮箱注册）
     */
    public function showRegistrationForm()
    {
        return view('max.auth.register');
    }

    /**
     * 注册处理（邮箱注册）
     */
    public function register(Request $request)
    {
        $request->validate([
            'email' => 'required|email|unique:users,email',
            'password' => 'required|min:6|confirmed',
            'name' => 'nullable|string|max:50',
        ]);

        $email = $request->email;
        
        // 创建新用户
        $user = User::create([
            'name' => $request->name ?? explode('@', $email)[0],
            'email' => $email,
            'password' => bcrypt($request->password),
            'role' => 'user',
            'email_verified_at' => now(),
        ]);
        
        Auth::login($user);
        
        // 记录注册来源
        Log::info('新用户注册', [
            'user_id' => $user->id,
            'email' => $email,
            'source' => $request->source ?? 'web',
        ]);
        
        return redirect()->route('max.home')
            ->with('success', '注册成功！');
    }
}
PHPEOF
```

### 步骤 4：清除缓存

```bash
# 清除视图缓存
docker exec ai_side_php_max php artisan view:clear

# 清除路由缓存
docker exec ai_side_php_max php artisan route:clear

# 清除配置缓存
docker exec ai_side_php_max php artisan config:clear

# 清除所有缓存
docker exec ai_side_php_max php artisan cache:clear
```

---

## ✅ 验证步骤

### 1. 访问注册页面

```
http://127.0.0.1:8082/register
```

**应该显示：**
- ✅ 邮箱输入框
- ✅ 密码输入框
- ✅ 确认密码输入框
- ✅ 昵称（可选）
- ❌ 不显示：手机号、验证码

### 2. 测试注册流程

1. 输入邮箱：`test@example.com`
2. 输入密码：`123456`
3. 确认密码：`123456`
4. 点击"同意协议并注册"
5. 应该注册成功并跳转到首页

---

## 📊 修改对比

| 项目 | 修改前 | 修改后 |
|------|--------|--------|
| 注册方式 | 手机号 + 验证码 | 邮箱 + 密码 |
| 验证方式 | 短信验证码 | 邮箱唯一性 |
| 密码 | 随机生成 | 用户自设 |
| 登录方式 | 手机号 | 邮箱 |

---

## 🔧 一键执行脚本

```bash
#!/bin/bash
# 注册页面修改为邮箱注册 - 一键执行

CONTAINER="ai_side_php_max"

echo "🔧 开始修改注册页面为邮箱注册..."

# 1. 备份原文件
echo "1️⃣ 备份原文件..."
docker exec $CONTAINER cp /var/www/html/resources/views/max/auth/register.blade.php /var/www/html/resources/views/max/auth/register.blade.php.bak
docker exec $CONTAINER cp /var/www/html/app/Http/Controllers/Auth/RegisterController.php /var/www/html/app/Http/Controllers/Auth/RegisterController.php.bak

# 2. 创建新的注册页面（上面已提供）
echo "2️⃣ 创建新的注册页面..."
# (粘贴上面的 cat 命令)

# 3. 修改控制器（上面已提供）
echo "3️⃣ 修改控制器..."
# (粘贴上面的 cat 命令)

# 4. 清除缓存
echo "4️⃣ 清除缓存..."
docker exec $CONTAINER php artisan view:clear
docker exec $CONTAINER php artisan route:clear
docker exec $CONTAINER php artisan config:clear
docker exec $CONTAINER php artisan cache:clear

echo "✅ 注册页面修改完成！"
echo ""
echo "📋 验证步骤："
echo "1. 访问注册页面：http://127.0.0.1:8082/register"
echo "2. 测试邮箱注册流程"
```

---

_文档生成时间：2026-04-02 14:55_  
_状态：待执行_
