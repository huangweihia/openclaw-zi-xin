# AI 副业情报局 MAX - 项目启动清单

> 创建日期：2026-04-01  
> 项目路径：`/home/node/.openclaw/workspace/ai-side-laravel-max`

---

## 一、环境准备

### 1.1 数据库配置

```sql
-- 1. 创建数据库
CREATE DATABASE `ai-side-laravel-max` DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 2. 创建用户（可选）
CREATE USER 'max_user'@'localhost' IDENTIFIED BY 'max_password_2026';
GRANT ALL PRIVILEGES ON `ai-side-laravel-max`.* TO 'max_user'@'localhost';
FLUSH PRIVILEGES;
```

**检查项：**
- [ ] 数据库已创建
- [ ] 用户权限已配置
- [ ] 数据库连接测试通过

---

### 1.2 项目配置

**步骤：**

```bash
# 1. 进入项目目录
cd /home/node/.openclaw/workspace/ai-side-laravel-max

# 2. 安装 PHP 依赖
composer install --no-dev --optimize-autoloader

# 3. 生成应用密钥
php artisan key:generate

# 4. 设置权限
chmod -R 775 storage/
chmod -R 775 bootstrap/cache/
chmod -R 775 public/avatars
```

**检查项：**
- [ ] 依赖安装完成
- [ ] APP_KEY 已生成
- [ ] 目录权限正确

---

### 1.3 环境变量配置

**编辑 `.env` 文件：**

```bash
# 应用配置
APP_NAME="AI 副业情报局 MAX"
APP_ENV=local
APP_DEBUG=true
APP_URL=http://127.0.0.1:8082

# 数据库配置
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=ai-side-laravel-max
DB_USERNAME=root
DB_PASSWORD=你的数据库密码

# 邮件配置（复用 1.0）
MAIL_MAILER=smtp
MAIL_HOST=smtp.qq.com
MAIL_PORT=465
MAIL_USERNAME=2801359160@qq.com
MAIL_PASSWORD=uvxftlhiicvzdffa
MAIL_ENCRYPTION=ssl

# OpenClaw Webhook
OPENCLAW_WEBHOOK_TOKEN=openclaw-ai-fetcher-2026

# 企业微信（后续配置）
WECHAT_BOT_WEBHOOK_URL=
```

**检查项：**
- [ ] 数据库配置正确
- [ ] 邮件配置正确
- [ ] Webhook Token 已设置

---

## 二、数据库迁移

### 2.1 运行基础迁移（1.0 复用）

```bash
cd /home/node/.openclaw/workspace/ai-side-laravel-max

# 运行所有迁移
php artisan migrate
```

**预期创建的表：**

```
✅ users                          # 用户表
✅ articles                       # 文章表
✅ projects                       # 项目表
✅ jobs                           # 职位表
✅ categories                     # 分类表
✅ comments                       # 评论表
✅ favorites                      # 收藏表
✅ view_histories                 # 浏览历史
✅ email_settings                 # 邮件配置
✅ email_templates                # 邮件模板
✅ email_logs                     # 邮件日志
✅ email_subscriptions            # 邮件订阅
✅ orders                         # 订单表
✅ announcements                  # 公告表
✅ problem_feedbacks              # 问题反馈
✅ settings                       # 系统设置
✅ system_notifications           # 系统通知
```

**检查项：**
- [ ] 所有表创建成功
- [ ] 无迁移错误
- [ ] 索引正确创建

---

### 2.2 运行 MAX 新增迁移

**创建 MAX 专属迁移文件：**

```bash
# 创建 Premium 内容表
php artisan make:migration create_premium_content_tables

# 创建用户发布表
php artisan make:migration create_user_posts_table

# 创建广告表
php artisan make:migration create_advertisements_tables

# 创建推送表
php artisan make:migration create_push_notifications_table
```

**迁移文件位置：** `database/migrations/max/`

**检查项：**
- [ ] Premium 内容表创建
- [ ] 用户发布表创建
- [ ] 广告表创建
- [ ] 推送表创建

---

## 三、功能测试

### 3.1 用户系统测试

```
测试用例：
□ 邮箱注册流程
□ 邮箱验证码接收
□ 登录功能
□ 退出登录
□ 个人资料查看
□ 头像上传
□ 密码修改
```

**预期结果：**
- 注册成功并自动登录
- 验证码正常发送和验证
- 登录状态保持正常
- 头像上传成功

---

### 3.2 内容浏览测试

```
测试用例：
□ 文章列表页
□ 文章详情页
□ 项目列表页
□ 项目详情页
□ 分类筛选
□ 搜索功能
□ VIP 内容锁定
```

**预期结果：**
- 列表分页正常
- 详情页内容完整
- 筛选功能正常
- 搜索功能正常
- VIP 内容正确锁定

---

### 3.3 互动功能测试

```
测试用例：
□ 文章点赞
□ 文章评论
□ 评论回复
□ 收藏文章
□ 取消收藏
□ 浏览历史记录
□ 清空历史
```

**预期结果：**
- 点赞状态正确切换
- 评论发表成功
- 楼中楼回复正常
- 收藏/取消正常
- 历史记录自动记录

---

### 3.4 邮件系统测试

```
测试用例：
□ SMTP 连接测试
□ 邮件模板加载
□ 邮件发送测试
□ 邮件日志记录
```

**测试命令：**
```bash
php artisan tinker

# 测试邮件发送
Mail::raw('测试邮件内容', function($message) {
    $message->to('2801359160@qq.com')->subject('邮件测试');
});
```

**预期结果：**
- SMTP 连接成功
- 邮件正常发送
- 收件箱收到测试邮件
- 邮件日志记录正确

---

## 四、OpenClaw 集成

### 4.1 Webhook 测试

**发送测试请求：**

```bash
curl -X POST http://127.0.0.1:8082/api/openclaw/webhook \
  -H "Content-Type: application/json" \
  -H "X-API-Token: openclaw-ai-fetcher-2026" \
  -d '{
    "type": "articles",
    "items": [
      {
        "title": "测试文章",
        "url": "https://example.com/test",
        "summary": "这是一篇测试文章",
        "content": "测试内容"
      }
    ]
  }'
```

**预期响应：**
```json
{
  "success": true,
  "message": "成功保存 1 篇文章",
  "saved": 1
}
```

**检查项：**
- [ ] Webhook 路由正常
- [ ] Token 验证通过
- [ ] 数据正确保存
- [ ] 日志记录完整

---

### 4.2 定时任务配置

**编辑 `app/Console/Kernel.php`：**

```php
protected function schedule(Schedule $schedule): void
{
    // AI 文章采集（每 30 分钟）
    $schedule->command('ai:fetch-articles')
             ->everyThirtyMinutes()
             ->timezone('Asia/Shanghai');
    
    // GitHub 项目采集（每 30 分钟）
    $schedule->command('ai:fetch-projects')
             ->everyThirtyMinutes()
             ->timezone('Asia/Shanghai');
    
    // 每日日报推送（每天 09:00）
    $schedule->command('wechat:send-daily-digest')
             ->dailyAt('09:00')
             ->timezone('Asia/Shanghai');
}
```

**启动调度器：**

```bash
# 开发环境
php artisan schedule:work

# 生产环境（Cron）
* * * * * cd /home/node/.openclaw/workspace/ai-side-laravel-max && php artisan schedule:run >> /dev/null 2>&1
```

**检查项：**
- [ ] 定时任务配置正确
- [ ] 调度器正常运行
- [ ] 任务按时执行
- [ ] 执行日志正常

---

## 五、后台配置

### 5.1 Filament 后台访问

**访问地址：** `http://127.0.0.1:8082/admin`

**默认管理员：**
- 需要先创建管理员账号

**创建管理员：**

```bash
php artisan tinker

$user = \App\Models\User::create([
    'name' => 'Admin',
    'email' => 'admin@aifyqbj.com',
    'password' => bcrypt('admin_password'),
    'role' => 'admin',
]);
```

**检查项：**
- [ ] 后台可访问
- [ ] 管理员账号创建
- [ ] 登录后台成功
- [ ] 各资源管理正常

---

### 5.2 后台资源配置

**需要创建的 Resource：**

```bash
# Premium 内容
php artisan make:filament-resource SideHustleCase
php artisan make:filament-resource AiToolMonetization
php artisan make:filament-resource PrivateTrafficSop
php artisan make:filament-resource PremiumResource

# 用户发布
php artisan make:filament-resource UserPost

# 广告
php artisan make:filament-resource Advertisement
php artisan make:filament-resource AdSlot
```

**检查项：**
- [ ] Premium 内容 Resource 创建
- [ ] 用户发布 Resource 创建
- [ ] 广告 Resource 创建
- [ ] CRUD 功能正常

---

## 六、前端配置

### 6.1 MAX 主题页面

**复制 MAX 静态页：**

```bash
cp -r /home/node/.openclaw/workspace/ai-side-laravel/resources/views/max \
      /home/node/.openclaw/workspace/ai-side-laravel-max/resources/views/
```

**检查项：**
- [ ] 首页（home.blade.php）
- [ ] VIP 页（vip.blade.php）
- [ ] 价格页（pricing.blade.php）
- [ ] 案例页（cases/index.blade.php）

---

### 6.2 路由配置

**编辑 `routes/web.php`：**

```php
// MAX 首页
Route::get('/max', function() {
    return view('max.home');
})->name('max.home');

// MAX VIP 页
Route::get('/max/vip', function() {
    return view('max.vip');
})->name('max.vip');

// MAX 价格页
Route::get('/max/pricing', function() {
    return view('max.pricing');
})->name('max.pricing');
```

**检查项：**
- [ ] MAX 首页路由
- [ ] VIP 页路由
- [ ] 价格页路由
- [ ] 访问正常

---

## 七、企业微信配置

### 7.1 注册企业微信

**步骤：**

1. 访问 https://work.weixin.qq.com/
2. 注册企业（个人可注册）
3. 创建应用
4. 添加机器人
5. 获取 Webhook URL

### 7.2 配置 Webhook

**编辑 `.env`：**

```bash
WECHAT_BOT_WEBHOOK_URL=https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=YOUR_KEY
```

**测试推送：**

```bash
php artisan tinker

$bot = app(\App\Services\WechatBotService::class);
$bot->sendText('测试消息');
```

**检查项：**
- [ ] 企业微信注册完成
- [ ] 应用创建成功
- [ ] 机器人添加成功
- [ ] Webhook URL 配置
- [ ] 测试消息接收成功

---

## 八、上线前检查

### 8.1 生产环境配置

```bash
# 修改 .env
APP_ENV=production
APP_DEBUG=false

# 优化配置
php artisan config:cache
php artisan route:cache
php artisan view:cache
```

### 8.2 安全检查

```
□ 关闭调试模式
□ 检查文件权限
□ 配置 HTTPS
□ 设置防火墙规则
□ 数据库备份策略
□ 日志轮转配置
```

### 8.3 性能优化

```
□ 启用 OPcache
□ 配置 Redis 缓存
□ 启用 Gzip 压缩
□ 配置 CDN（可选）
□ 图片压缩
```

---

## 九、上线后监控

### 9.1 监控指标

| 指标 | 工具 | 阈值 |
|------|------|------|
| CPU 使用率 | htop | < 80% |
| 内存使用率 | free -h | < 80% |
| 磁盘使用率 | df -h | < 85% |
| 数据库连接 | MySQL | < 100 |
| 错误日志 | Laravel logs | 每日<10 个 |

### 9.2 备份策略

```bash
# 数据库备份（每日）
0 2 * * * mysqldump ai-side-laravel-max > /backup/max-$(date +\%Y\%m\%d).sql

# 文件备份（每周）
0 3 * * 0 tar -czf /backup/max-files-$(date +\%Y\%m\%d).tar.gz /home/node/.openclaw/workspace/ai-side-laravel-max/storage
```

---

## 十、问题排查

### 10.1 常见问题

| 问题 | 原因 | 解决方案 |
|------|------|---------|
| 邮件发送失败 | SMTP 配置错误 | 检查 .env 配置 |
| Webhook 401 | Token 不匹配 | 检查 Token 配置 |
| 权限错误 | 目录权限不足 | chmod 775 |
| 数据库连接失败 | 配置错误 | 检查 .env DB 配置 |

### 10.2 日志位置

```
storage/logs/laravel.log          # Laravel 日志
storage/logs/openclaw.log         # OpenClaw 日志
/var/log/nginx/error.log          # Nginx 日志
/var/log/php8.2-fpm.log           # PHP-FPM 日志
```

---

_最后更新：2026-04-01_

_版本：v1.0_
